{
  "name": "team-task-manager",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend_TaskManager run build",
    "start": "npm --prefix backend_TaskManager start",
    "dev:backend": "npm --prefix backend_TaskManager run dev",
    "dev:frontend": "npm --prefix frontend_TaskManager run dev"
  }
}
